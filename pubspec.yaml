name: flare_splash_screen
description: Facilitator for having a Splash screen with a Flare animation until some work has been done for the initialization of the app
version: 4.0.0
homepage: https://github.com/jaumard/flare_splash_screen
publish_to: 'none'

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.7.8 <2.0.0"

dependencies:
  flare_loading:
    git:
      url: https://github.com/minhazma/flare_loading.git
      ref: master
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
